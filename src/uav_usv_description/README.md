# uav_usv_description

Ownership: boat geometry and sensor mounting team.

Future contents:

- USV URDF/Xacro, visual meshes, collision meshes, mass and inertia.
- `base_link`, `mid360_link`, `imu_link`, camera and landing-pad frames.
- UAV/USV robot descriptions shared by Gazebo and RViz.

The active simulation model is maintained in `uav_usv_gazebo/models/simple_boat`.
